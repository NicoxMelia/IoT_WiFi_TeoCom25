name: dashboard_unc
description: Monitor de sensores ESP32
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.15.5
  # Utils
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  uuid: ^4.2.1
  # UI
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  phosphor_flutter: ^2.0.0
  flutter_colorpicker: ^1.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/Logo-UNC-azul.png